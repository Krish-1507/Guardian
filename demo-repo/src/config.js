// Hardcoded credentials — gitleaks flags this for real when installed.
const API_KEY = "AKIA1234567890ABCDEFGH"; // fake AWS access key id
const DB_PASSWORD = "supersecretpassword123";

module.exports = { API_KEY, DB_PASSWORD };
